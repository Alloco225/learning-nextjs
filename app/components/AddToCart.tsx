"use client";

import React from "react";

const AddToCart = () => {
	return <button onClick={() => console.log("hi there hello")} className="btn btn-primary">Click me</button>;
};

export default AddToCart;
